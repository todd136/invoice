   # 发票解析与识别逻辑说明

## 一、整体流程概览

发票解析采用 **PDF文本提取 + 表格解析 + 正则匹配** 的混合策略，主要流程如下：

```
PDF文件
  ↓
1. PDF文本提取（pdfplumber）
  ├─ 提取单词（extract_words）
  ├─ 过滤红色印章文字
  └─ 提取表格数据（extract_table）
  ↓
2. 文本清洗与验证
  ├─ 清理乱码字符
  ├─ 验证文本有效性
  └─ 检测乱码
  ↓
3. 基本信息提取
  ├─ 发票号码
  ├─ 开票日期
  ├─ 购买方名称
  ├─ 购买方税号
  └─ 发票类型
  ↓
4. 商品明细解析
  ├─ 从表格中解析（主要方案）
  └─ 从文本中解析（备选方案）
  ↓
5. 返回Invoice对象
```

---

## 二、详细解析逻辑

### 2.1 PDF文本提取阶段

#### 2.1.1 文本提取参数

**位置**: `invoice_service.py:44-49`

```python
words = page.extract_words(
    x_tolerance=5,      # 水平容差：5像素，帮助合并同一行的字符
    y_tolerance=2,      # 垂直容差：2像素
    keep_blank_chars=False,
    use_text_flow=True  # 使用文本流逻辑重组，解决乱序
)
```

**作用**:
- `x_tolerance=5`: 增大水平容差，帮助合并被拆分的字符（如 '税', '额' 合并为 '税额'）
- `use_text_flow=True`: 使用文本流逻辑重组，解决PDF文本乱序问题

#### 2.1.2 红色印章过滤

**位置**: `invoice_service.py:111-172` (`filter_seal_text`)

**过滤逻辑**:
1. **字符位置匹配**: 通过边界框坐标匹配单词对应的字符
2. **颜色检测**: 检查字符的 `non_stroking_color`（填充色）和 `stroking_color`（描边色）
3. **红色判断标准**:
   - **RGB格式**: `R > 0.6 且 G < 0.4 且 B < 0.4`
   - **CMYK格式**: `C < 0.2 且 M > 0.5`
4. **过滤规则**: 如果单词中任何一个字符是红色，则过滤掉整个单词

**示例**: 过滤掉印章中的 "深圳市"、"税务局" 等红色文字

#### 2.1.3 表格提取参数

**位置**: `invoice_service.py:55-62`

```python
table = page.extract_table(table_settings={
    "vertical_strategy": "text",      # 垂直策略：基于文本对齐
    "horizontal_strategy": "text",    # 水平策略：基于文本对齐
    "snap_x_tolerance": 8,             # 水平合并容差：8像素（帮助合并列）
    "snap_y_tolerance": 3,             # 垂直合并容差：3像素
    "join_tolerance": 15,              # 字符合并容差：15像素（帮助合并数字和小数点）
    "min_words_vertical": 0,
})
```

**作用**:
- `snap_x_tolerance=8`: 帮助合并被拆分的列（如 '税', '额' 合并为 '税额'）
- `join_tolerance=15`: 帮助合并数字和小数点（如 '16', '.98' 合并为 '16.98'）

---

### 2.2 文本清洗与验证

#### 2.2.1 文本清洗

**位置**: `text_utils.py:8-51` (`clean_text`)

**保留的字符**:
- ASCII 可打印字符（32-126）
- 中文字符（CJK统一汉字、扩展A、符号和标点）
- 全角字符
- 换行符、制表符

**移除的字符**:
- 替换字符（U+FFFD，编码错误标记）
- 乱码字符范围（0xFFF0-0xFFFF, 0xD800-0xDFFF）

#### 2.2.2 文本有效性验证

**位置**: `text_utils.py:54-75` (`is_all_text_valid`)

**判断标准**: 可打印字符占比 >= 30%

#### 2.2.3 乱码检测

**位置**: `text_utils.py:78-108` (`is_contain_garbled_text`)

**判断标准**:
- 不包含中文字符
- 不包含发票关键词（发票、号码、日期等）
- 特殊符号占比 > 20%

---

### 2.3 基本信息提取

#### 2.3.1 发票号码提取

**位置**: `invoice_service.py:209-229` (`extract_invoice_code`)

**提取策略（优先级递减）**:

1. **严格模式**: `INVOICE_CODE_REGEX`
   - 正则: `r'发票(?:号码|编码)?[：:\s]*(\d{12,20})'`
   - 匹配: "发票号码：1234567890123456"

2. **宽松模式**: `INVOICE_CODE_REGEX_LOOSE`
   - 正则: `r'(?s)发票(?:号码|编码)?[^0-9]*(\d{12,20})'`
   - 允许中间有非数字字符

3. **回退方案**: 查找所有12-20位数字，选择最长的

**正则定义**: `regex_utils.py:9-10`

#### 2.3.2 开票日期提取

**位置**: `invoice_service.py:232-244` (`extract_date`)

**提取策略**:

1. **严格模式**: `DATE_REGEX`
   - 正则: `r'开票日期[：:\s]*(\d{4}年\d{1,2}月\d{1,2}日)'`
   - 匹配: "开票日期：2024年12月24日"

2. **宽松模式**: `DATE_REGEX_LOOSE`
   - 正则: `r'\d{4}\s*年\s*\d{1,2}\s*月\s*\d{1,2}\s*日'`
   - 允许空格

**正则定义**: `regex_utils.py:13-14`

#### 2.3.3 购买方名称提取

**位置**: `invoice_service.py:247-299` (`extract_buyer_name`)

**提取策略（优先级递减）**:

1. **策略1**: 从"名称："后面提取
   - 正则: `r'名称[：:]\s*([^\n]+?)(?:\s*(?:统一社会信用代码|纳税人识别号)|$)'`
   - 截取到关键词（统一社会信用代码、纳税人识别号等）之前
   - 排除税号和纯数字

2. **策略2**: 匹配公司名称模式
   - 正则: `r'([\u4e00-\u9fa5]+(?:（[^）]+）)?[\u4e00-\u9fa5]*(?:公司|企业|有限|股份|集团|个体工商户))'`
   - 匹配包含"公司"、"企业"等关键词的中文名称

3. **策略3**: 匹配房间号
   - 正则: `r'\b\d+-\d+(?:-\d+)?\b'` 或 `r'\b[A-Za-z]?\d{3,6}\b'`
   - 匹配格式如 "101-201" 或 "A101"

4. **策略4**: 查找最长的中文文本（排除税号和关键词）

**正则定义**: `regex_utils.py:32-34`

#### 2.3.4 购买方税号提取

**位置**: `invoice_service.py:302-344` (`extract_buyer_tax_id`)

**提取策略**:

1. **模式匹配**: 使用6种不同的正则模式
   ```python
   TAX_ID_PATTERNS = [
       r'统一社会信用代码[：:\s]*([A-Za-z0-9]{15,20})',
       r'纳税人识别号[：:\s]*([A-Za-z0-9]{15,20})',
       # ... 更多模式
   ]
   ```

2. **全局扫描**: 查找所有15-20位的字母数字组合
   - 正则: `r'[A-Za-z0-9]{15,20}'`

3. **验证与评分**:
   - 长度验证: 15-20位
   - 如果是19位，尝试去头/去尾得到18位
   - 使用 `is_tax_id()` 验证是否为有效的统一社会信用代码
   - 使用 `score_candidate()` 评分，选择得分最高的

**税号验证逻辑** (`number_utils.py:8-58`):
- 长度必须是18位
- 第1位：登记管理部门代码（数字）
- 第2位：机构类别代码（数字）
- 第3-8位：登记管理机关行政区划码（6位数字）
- 第9-17位：主体标识码（9位，数字或大写字母，排除I、O、Z、S、V）
- 第18位：校验码（数字或字母）
- 必须包含至少一个字母（不是纯数字）

**正则定义**: `regex_utils.py:20-30`

#### 2.3.5 发票类型提取

**位置**: `invoice_service.py:347-365` (`extract_invoice_type`)

**识别逻辑**:

1. **普通发票**: 
   - 包含 "（普通发票）" 或 "(普通发票)"
   - 或同时包含 "普通"/"普" 和 "发票"

2. **增值税专用发票**:
   - 包含 "（增值税专用发票）" 或 "(增值税专用发票)"
   - 或包含 "增值税专用发票" 或 "专用发票"
   - 或同时包含 "增值税"、"专用" 和 "发票"

**返回值**: '普' 或 '专' 或 ''

---

### 2.4 商品明细解析

#### 2.4.1 从表格中解析（主要方案）

**位置**: `invoice_service.py:368-525` (`parse_line_items_from_table`)

**解析流程**:

1. **表头识别**:
   - 查找包含至少3个表头关键词的行
   - 表头关键词: `['项目名称', '规格型号', '单位', '数量', '单价', '金额', '税率', '税额']`
   - 记录表头行索引

2. **列索引映射**:
   - 遍历表头行的每个单元格
   - 识别各列对应的字段（项目名称、规格型号、单位等）
   - 建立列索引到字段的映射

3. **数据行处理**:
   - 遍历表头行之后的所有行
   - 跳过合计行、备注行等（包含 "合计"、"价税合计"、"备注"、"开票人"、"¥"）

4. **项目名称匹配**:
   - **严格匹配**: 使用 `PROJECT_NAME_REGEX` 匹配 `*项目名称*费用名称` 格式
     - 正则: `r'\*[^*]+\*[^*]+'`
   - **放宽匹配**: 如果严格匹配失败
     - 包含中文字符
     - 不是纯数字
     - 长度在 2-100 字符之间

5. **字段提取**:
   - 根据列索引映射提取各字段
   - 使用 `clean_garbled_chars()` 清理乱码字符
   - 提取的字段: 规格型号、单位、数量、单价、金额、税率、税额

#### 2.4.2 从文本中解析（备选方案）

**位置**: `invoice_service.py:528-570` (`parse_line_items_from_text`)

**触发条件**: 表格解析失败时自动调用

**解析流程**:

1. **标准格式匹配**: 使用 `PROJECT_NAME_REGEX` 查找 `*项目名称*费用名称` 格式

2. **关键词匹配**: 如果标准格式失败
   - 查找包含 "项目" 关键词的行
   - 长度在 5-200 字符之间
   - 再次尝试匹配项目名称模式

3. **创建明细项**: 为每个匹配创建 `LineItem` 对象（仅包含项目名称）

---

## 三、数据模型

### 3.1 Invoice 对象

**位置**: `invoice.py:21-35`

```python
@dataclass
class Invoice:
    name: str = ""              # 发票文件名
    code: str = ""               # 发票编码
    date: str = ""               # 日期
    buyer: str = ""              # 购买方
    buyer_tax_id: str = ""       # 购买方统一社会信用代码/纳税人识别号
    invoice_type: str = ""       # 发票类型（普通/增值税专用）
    items: List[LineItem] = []   # 商品明细列表
```

### 3.2 LineItem 对象

**位置**: `invoice.py:8-19`

```python
@dataclass
class LineItem:
    item_name: str = ""    # 项目名称
    spec: str = ""         # 规格
    unit: str = ""         # 单位
    quantity: str = ""     # 数量
    price: str = ""        # 单价
    amount: str = ""       # 金额
    tax_rate: str = ""     # 税率
    tax_amount: str = ""   # 税额
```

---

## 四、关键正则表达式汇总

### 4.1 发票号码

```python
INVOICE_CODE_REGEX = r'发票(?:号码|编码)?[：:\s]*(\d{12,20})'
INVOICE_CODE_REGEX_LOOSE = r'(?s)发票(?:号码|编码)?[^0-9]*(\d{12,20})'
LONG_NUMBER_REGEX = r'\d{12,20}'
```

### 4.2 日期

```python
DATE_REGEX = r'开票日期[：:\s]*(\d{4}年\d{1,2}月\d{1,2}日)'
DATE_REGEX_LOOSE = r'\d{4}\s*年\s*\d{1,2}\s*月\s*\d{1,2}\s*日'
```

### 4.3 项目名称

```python
PROJECT_NAME_REGEX = r'\*[^*]+\*[^*]+'  # *项目名称*费用名称
```

### 4.4 税号

```python
TAX_ID_PATTERNS = [
    r'统一社会信用代码[：:\s]*([A-Za-z0-9]{15,20})',
    r'纳税人识别号[：:\s]*([A-Za-z0-9]{15,20})',
    # ... 更多模式
]
TAX_ID_GLOBAL_REGEX = r'[A-Za-z0-9]{15,20}'
```

### 4.5 公司名称

```python
COMPANY_NAME_REGEX = r'([\u4e00-\u9fa5]+(?:（[^）]+）)?[\u4e00-\u9fa5]*(?:公司|企业|有限|股份|集团|个体工商户))'
```

### 4.6 房间号

```python
ROOM_NUMBER_REGEX = r'\b\d+-\d+(?:-\d+)?\b'
ROOM_ALPHA_REGEX = r'\b[A-Za-z]?\d{3,6}\b'
```

---

## 五、错误处理与日志

### 5.1 错误处理策略

1. **文本提取失败**: 抛出异常 "无法从PDF中提取有效文本"
2. **乱码检测**: 抛出异常 "提取的文本可能是乱码"
3. **明细解析失败**: 
   - 首先尝试从表格解析
   - 如果失败，尝试从文本解析
   - 如果都失败，抛出异常 "未能解析到发票明细数据"

### 5.2 日志级别

- **INFO**: 处理开始、完成、解析结果统计
- **WARNING**: 解析失败、备选方案触发
- **DEBUG**: 详细的解析过程、匹配结果、数据行内容
- **ERROR**: 异常错误

---

## 六、优化点与改进

### 6.1 已实现的优化

1. **红色印章过滤**: 通过颜色检测过滤印章文字
2. **列合并优化**: 调整 `snap_x_tolerance` 和 `join_tolerance` 参数，改善列拆分问题
3. **项目名称匹配放宽**: 不仅匹配标准格式，还支持包含中文的文本
4. **备选解析方案**: 表格解析失败时自动尝试文本解析
5. **详细日志**: 提供丰富的调试信息

### 6.2 可能的改进方向

1. **OCR支持**: 对于扫描版PDF，可以集成OCR功能
2. **机器学习**: 使用ML模型提高字段识别准确率
3. **模板匹配**: 针对不同发票格式使用不同的解析模板
4. **字段验证**: 增加字段格式验证（如日期格式、金额格式等）

---

## 七、使用示例

```python
from src.invoice.invoice_service import extract_invoice_by_table_and_text

# 解析发票
invoice = extract_invoice_by_table_and_text("/path/to/invoice.pdf")

# 访问基本信息
print(f"发票号码: {invoice.code}")
print(f"开票日期: {invoice.date}")
print(f"购买方: {invoice.buyer}")
print(f"税号: {invoice.buyer_tax_id}")
print(f"发票类型: {invoice.invoice_type}")

# 访问商品明细
for item in invoice.items:
    print(f"项目: {item.item_name}, 金额: {item.amount}")
```

---

## 八、文件结构

```
src/invoice/
├── invoice_service.py    # 主解析服务（核心逻辑）
├── invoice.py            # 数据模型定义
├── regex_utils.py        # 正则表达式工具
├── text_utils.py         # 文本处理工具
├── number_utils.py       # 数字处理工具（税号验证等）
├── excel_service.py      # Excel导出服务
└── file_service.py      # 文件操作服务
```


