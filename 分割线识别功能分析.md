# 基于分割线的发票区域识别可行性分析

## 一、pdfplumber识别线条的能力

### 1.1 ✅ pdfplumber支持识别线条

**pdfplumber提供了以下API来访问PDF中的图形元素**：

1. **`page.lines`**: 获取页面中的所有线条
   ```python
   lines = page.lines
   # 返回列表，每个元素包含：
   {
       'x0': 100.0,    # 起点X坐标
       'y0': 50.0,     # 起点Y坐标
       'x1': 200.0,    # 终点X坐标
       'y1': 50.0,     # 终点Y坐标
       'width': 100.0, # 线条宽度
       'height': 0.0,  # 线条高度（水平线为0）
       'linewidth': 0.5,  # 线条粗细
       'stroking_color': (0, 0, 0),  # 线条颜色（RGB）
   }
   ```

2. **`page.rects`**: 获取页面中的所有矩形
   ```python
   rects = page.rects
   # 返回列表，每个元素包含：
   {
       'x0': 100.0,    # 左边界
       'y0': 50.0,     # 上边界
       'x1': 200.0,    # 右边界
       'y1': 100.0,    # 下边界
       'width': 100.0,
       'height': 50.0,
   }
   ```

3. **`page.curves`**: 获取页面中的曲线

4. **`page.edges`**: 获取页面中的边（包括线条和矩形边）

### 1.2 线条类型识别

**pdfplumber可以识别**：
- ✅ 水平线（horizontal lines）
- ✅ 垂直线（vertical lines）
- ✅ 斜线（diagonal lines）
- ✅ 矩形边框（rectangles）
- ✅ 表格线（table borders）

**线条属性**：
- 坐标信息（起点、终点）
- 线条粗细（linewidth）
- 线条颜色（stroking_color）
- 线条类型（实线、虚线等）

---

## 二、发票中的分割线特征分析

### 2.1 发票典型布局

```
┌─────────────────────────────────────────┐
│  发票头区域（无表格线）                    │
│  发票号码、开票日期、发票类型              │
├─────────────────────────────────────────┤ ← 分割线1：发票头与购销方
│  购买方信息    │    销售方信息            │
│  ┌─────────┐  │  ┌─────────┐           │
│  │ 名称    │  │  │ 名称    │           │
│  │ 税号    │  │  │ 税号    │           │
│  └─────────┘  │  └─────────┘           │
├─────────────────────────────────────────┤ ← 分割线2：购销方与表格
│  商品明细表格区域                          │
│  ┌──────────────────────────────────┐ │
│  │ 项目名称 │ 规格 │ 数量 │ 金额 │ ... │ │
│  ├──────────────────────────────────┤ │
│  │ *项目1*  │ ...  │ ...  │ ...  │ ... │ │
│  │ *项目2*  │ ...  │ ...  │ ...  │ ... │ │
│  └──────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 2.2 关键分割线识别

#### 2.2.1 分割线1：发票头与购销方分界线

**特征**：
- 水平线，横跨整个页面宽度
- Y坐标位置：在发票头区域下方
- 通常较粗（linewidth > 0.5）
- 位置相对固定（在发票号码、开票日期下方）

**识别方法**：
```python
# 查找横跨页面宽度的水平线
page_width = page.width
header_bottom_line = None

for line in page.lines:
    # 水平线（y0 ≈ y1）
    if abs(line['y0'] - line['y1']) < 1.0:
        # 横跨大部分页面宽度（>80%）
        line_width = abs(line['x1'] - line['x0'])
        if line_width > page_width * 0.8:
            # 检查是否在发票头关键词下方
            if is_below_header_keywords(line, words):
                header_bottom_line = line
                break
```

#### 2.2.2 分割线2：购销方与表格分界线

**特征**：
- 水平线，横跨整个页面宽度
- Y坐标位置：在购销方区域下方、表格表头上方
- 通常较粗
- 位置：在"项目名称"等表头关键词上方

**识别方法**：
```python
# 查找表格上方的分割线
table_top_line = None

for line in page.lines:
    if abs(line['y0'] - line['y1']) < 1.0:  # 水平线
        line_width = abs(line['x1'] - line['x0'])
        if line_width > page_width * 0.8:
            # 检查是否在表头关键词上方
            if is_above_table_header(line, words):
                table_top_line = line
                break
```

#### 2.2.3 购买方与销售方分界线

**特征**：
- 垂直线，位于页面中间
- X坐标位置：约在页面宽度的一半
- 可能不完整（只存在于购销方区域）

**识别方法**：
```python
# 查找页面中间的垂直线
page_width = page.width
mid_x = page_width / 2
buyer_seller_divider = None

for line in page.lines:
    # 垂直线（x0 ≈ x1）
    if abs(line['x0'] - line['x1']) < 1.0:
        # 在页面中间位置（±10%容差）
        if abs(line['x0'] - mid_x) < page_width * 0.1:
            # 在购销方区域内
            if header_bottom_line and table_top_line:
                if (line['y0'] < header_bottom_line['y0'] and 
                    line['y1'] > table_top_line['y0']):
                    buyer_seller_divider = line
                    break
```

#### 2.2.4 表格内部线条

**特征**：
- 表格边框：矩形或完整的线条网格
- 表头行下方的水平线
- 列之间的垂直线

**识别方法**：
```python
# 使用pdfplumber的表格提取功能
table = page.extract_table()
# 表格提取已经利用了线条信息
```

---

## 三、基于分割线的区域识别方案

### 3.1 实现思路

```python
def partition_invoice_by_lines(page, words):
    """
    通过分割线识别发票区域
    
    Returns:
        (header_words, buyer_words, seller_words, table_words)
    """
    # 1. 提取所有线条
    lines = page.lines
    page_width = page.width
    page_height = page.height
    
    # 2. 识别关键分割线
    header_bottom_line = find_header_bottom_line(lines, words, page_width)
    table_top_line = find_table_top_line(lines, words, page_width)
    buyer_seller_divider = find_buyer_seller_divider(
        lines, header_bottom_line, table_top_line, page_width
    )
    
    # 3. 根据分割线分配单词
    header_words = []
    buyer_words = []
    seller_words = []
    table_words = []
    
    for word in words:
        word_y = word['top']
        word_x = word['x0']
        
        # 发票头区域：在header_bottom_line上方
        if header_bottom_line and word_y > header_bottom_line['y0']:
            header_words.append(word)
        
        # 表格区域：在table_top_line下方
        elif table_top_line and word_y < table_top_line['y0']:
            table_words.append(word)
        
        # 购销方区域：在header_bottom_line和table_top_line之间
        elif (header_bottom_line and table_top_line and
              header_bottom_line['y0'] > word_y > table_top_line['y0']):
            # 根据buyer_seller_divider区分购买方和销售方
            if buyer_seller_divider:
                if word_x < buyer_seller_divider['x0']:
                    buyer_words.append(word)
                else:
                    seller_words.append(word)
            else:
                # 如果没有分界线，使用X坐标中点
                mid_x = page_width / 2
                if word_x < mid_x:
                    buyer_words.append(word)
                else:
                    seller_words.append(word)
    
    return header_words, buyer_words, seller_words, table_words
```

### 3.2 关键分割线识别函数

#### 3.2.1 识别发票头底部分割线

```python
def find_header_bottom_line(lines, words, page_width):
    """
    查找发票头与购销方的分界线
    """
    header_keywords = ['发票号码', '开票日期', '电子发票', '普通发票']
    
    # 找到发票头关键词的Y坐标范围
    header_y_coords = []
    for word in words:
        if any(kw in word['text'] for kw in header_keywords):
            header_y_coords.append(word['top'])
    
    if not header_y_coords:
        return None
    
    header_y_min = min(header_y_coords)  # 最下方的关键词
    
    # 查找在关键词下方、横跨页面的水平线
    candidates = []
    for line in lines:
        # 水平线
        if abs(line['y0'] - line['y1']) < 1.0:
            line_width = abs(line['x1'] - line['x0'])
            # 横跨大部分页面
            if line_width > page_width * 0.7:
                # 在关键词下方（容差10像素）
                if header_y_min - 20 < line['y0'] < header_y_min + 10:
                    candidates.append(line)
    
    if candidates:
        # 选择最接近关键词的线
        candidates.sort(key=lambda l: abs(l['y0'] - header_y_min))
        return candidates[0]
    
    return None
```

#### 3.2.2 识别表格顶部分割线

```python
def find_table_top_line(lines, words, page_width):
    """
    查找购销方与表格的分界线
    """
    table_keywords = ['项目名称', '规格型号', '单位', '数量', '单价', '金额']
    
    # 找到表头关键词的Y坐标
    table_header_y = None
    for word in words:
        if any(kw in word['text'] for kw in table_keywords):
            if table_header_y is None or word['top'] > table_header_y:
                table_header_y = word['top']
    
    if not table_header_y:
        return None
    
    # 查找在表头上方、横跨页面的水平线
    candidates = []
    for line in lines:
        if abs(line['y0'] - line['y1']) < 1.0:  # 水平线
            line_width = abs(line['x1'] - line['x0'])
            if line_width > page_width * 0.7:
                # 在表头上方（容差10像素）
                if table_header_y - 10 < line['y0'] < table_header_y + 20:
                    candidates.append(line)
    
    if candidates:
        candidates.sort(key=lambda l: abs(l['y0'] - table_header_y))
        return candidates[0]
    
    return None
```

#### 3.2.3 识别购买方/销售方分界线

```python
def find_buyer_seller_divider(lines, header_bottom_line, table_top_line, page_width):
    """
    查找购买方与销售方的分界线（垂直线）
    """
    if not header_bottom_line or not table_top_line:
        return None
    
    mid_x = page_width / 2
    y_range = (table_top_line['y0'], header_bottom_line['y0'])
    
    candidates = []
    for line in lines:
        # 垂直线
        if abs(line['x0'] - line['x1']) < 1.0:
            # 在页面中间位置
            if abs(line['x0'] - mid_x) < page_width * 0.15:
                # 在购销方区域内
                line_y_range = (min(line['y0'], line['y1']), 
                               max(line['y0'], line['y1']))
                if (line_y_range[0] < y_range[1] and 
                    line_y_range[1] > y_range[0]):
                    candidates.append(line)
    
    if candidates:
        # 选择最接近页面中点的线
        candidates.sort(key=lambda l: abs(l['x0'] - mid_x))
        return candidates[0]
    
    return None
```

---

## 四、可行性分析

### 4.1 ✅ 完全可行

**优势**：

1. **直观明确**：
   - 分割线是发票布局的明确标识
   - 比纯坐标分割更可靠
   - 不依赖关键词匹配

2. **pdfplumber支持完善**：
   - `page.lines` 提供所有线条信息
   - 包含完整的坐标和属性信息
   - 可以识别水平线、垂直线、矩形等

3. **实现简单**：
   - 逻辑清晰：找到分割线 → 根据分割线分配文本
   - 代码量少，易于维护

4. **准确性高**：
   - 分割线是PDF的原始元素，不会因文本提取问题而丢失
   - 即使文本乱序，分割线位置仍然准确

### 4.2 潜在问题与解决方案

#### 4.2.1 问题1：分割线可能缺失或不完整

**场景**：
- 某些发票可能没有明显的分割线
- 分割线可能被扫描PDF识别为图像而非线条
- 分割线可能不完整（只画了一部分）

**解决方案**：
```python
# 混合策略：优先使用分割线，失败时回退到坐标分割
def partition_invoice_zones(page, words, table):
    # 尝试使用分割线
    header_words, buyer_words, seller_words, table_words = (
        partition_by_lines(page, words)
    )
    
    # 验证分区结果
    if not header_words or not buyer_words:
        # 回退到坐标分割
        logging.warning('分割线识别失败，使用坐标分割')
        return partition_by_coordinates(words, table, page)
    
    return header_words, buyer_words, seller_words, table_words
```

#### 4.2.2 问题2：线条识别可能包含无关线条

**场景**：
- 印章边框
- 装饰性线条
- 表格内部线条

**解决方案**：
```python
# 过滤无关线条
def filter_relevant_lines(lines, page_width, page_height):
    """
    过滤出关键分割线
    """
    relevant_lines = []
    
    for line in lines:
        # 1. 过滤太短的线（可能是装饰）
        line_length = math.sqrt(
            (line['x1'] - line['x0'])**2 + (line['y1'] - line['y0'])**2
        )
        if line_length < page_width * 0.3:
            continue
        
        # 2. 过滤太细的线（可能是表格内部线）
        if line.get('linewidth', 0) < 0.3:
            continue
        
        # 3. 过滤红色线条（可能是印章）
        color = line.get('stroking_color')
        if color and len(color) == 3:
            if color[0] > 0.6 and color[1] < 0.4 and color[2] < 0.4:
                continue  # 红色线条，可能是印章
        
        relevant_lines.append(line)
    
    return relevant_lines
```

#### 4.2.3 问题3：线条坐标容差处理

**场景**：
- 线条坐标可能有微小误差
- 文本可能不完全对齐到线条

**解决方案**：
```python
# 使用容差判断文本是否在区域内
LINE_TOLERANCE = 5.0  # 5像素容差

def is_in_region(word, line_y, tolerance=LINE_TOLERANCE):
    """
    判断单词是否在分割线划分的区域内
    """
    return abs(word['top'] - line_y) < tolerance
```

### 4.3 与坐标分割方法对比

| 特性 | 分割线方法 | 坐标分割方法 |
|------|-----------|------------|
| **可靠性** | ⭐⭐⭐⭐⭐ 高（线条是PDF原始元素） | ⭐⭐⭐⭐ 中高（依赖文本坐标） |
| **实现复杂度** | ⭐⭐⭐ 简单（逻辑清晰） | ⭐⭐⭐⭐ 中等（需要计算边界） |
| **适应性** | ⭐⭐⭐ 中（需要PDF有分割线） | ⭐⭐⭐⭐⭐ 高（适用于所有PDF） |
| **准确性** | ⭐⭐⭐⭐⭐ 高（线条位置准确） | ⭐⭐⭐⭐ 中高（可能受文本乱序影响） |
| **维护性** | ⭐⭐⭐⭐⭐ 高（代码简单） | ⭐⭐⭐ 中（逻辑较复杂） |

### 4.4 推荐方案：混合策略

**最佳实践**：结合分割线和坐标两种方法

```python
def partition_invoice_zones_hybrid(page, words, table):
    """
    混合策略：优先使用分割线，失败时使用坐标分割
    """
    # 1. 尝试使用分割线
    try:
        result = partition_by_lines(page, words)
        # 验证结果
        if validate_partition(result):
            return result
    except Exception as e:
        logging.debug(f'分割线识别失败: {e}')
    
    # 2. 回退到坐标分割
    logging.info('使用坐标分割作为备选方案')
    return partition_by_coordinates(words, table, page)
```

**优势**：
- ✅ 充分利用PDF的结构信息（分割线）
- ✅ 提供备选方案（坐标分割）
- ✅ 适应不同类型的发票

---

## 五、实现建议

### 5.1 实现步骤

1. **阶段1：基础线条识别**
   - 实现 `find_header_bottom_line()` 函数
   - 实现 `find_table_top_line()` 函数
   - 实现 `find_buyer_seller_divider()` 函数
   - 测试识别准确性

2. **阶段2：区域分配**
   - 实现 `partition_by_lines()` 函数
   - 根据分割线分配单词到各区域
   - 验证分配准确性

3. **阶段3：混合策略**
   - 实现坐标分割作为备选
   - 实现混合策略函数
   - 对比两种方法的准确性

### 5.2 代码结构

```python
# invoice_service.py

def extract_invoice_by_table_and_text(pdf_file_path: str) -> Invoice:
    # ... 现有代码 ...
    
    # 尝试使用分割线分区
    try:
        header_words, buyer_words, seller_words, table_words = (
            partition_invoice_zones_hybrid(page, clean_words, table)
        )
        # 从分区文本中提取信息
        code, date, buyer, buyer_tax_id, invoice_type = (
            extract_basic_info_from_zones(
                header_words, buyer_words, cleaned_text, full_text
            )
        )
    except Exception as e:
        logging.warning(f'分区提取失败，使用全文本提取: {e}')
        # 回退到现有方案
        code, date, buyer, buyer_tax_id, invoice_type = extract_basic_info(
            clean_words, cleaned_text, full_text
        )
```

### 5.3 测试要点

1. **线条识别测试**：
   - 测试不同格式发票的线条识别
   - 测试缺失线条时的容错
   - 测试线条不完整时的处理

2. **区域分配测试**：
   - 验证各区域文本分配准确性
   - 测试边界情况的处理

3. **准确性对比**：
   - 对比分割线方法和坐标分割方法
   - 对比与现有全文本提取方法

---

## 六、总结

### 6.1 可行性结论

✅ **完全可行** - pdfplumber完全支持识别PDF中的线条，可以通过分割线准确识别发票的不同区域。

### 6.2 优势

1. **直观可靠**：分割线是PDF的原始结构元素，位置准确
2. **实现简单**：逻辑清晰，代码量少
3. **准确性高**：不受文本提取乱序影响

### 6.3 建议

**推荐实现基于分割线的区域识别**，理由：
1. ✅ pdfplumber完全支持
2. ✅ 实现简单，维护成本低
3. ✅ 准确性高，可靠性好
4. ✅ 可以与坐标分割方法结合，提供更好的容错性

**实现优先级**: **高**（比坐标分割更简单、更可靠）

**最佳实践**: 使用**混合策略**，优先分割线，失败时回退到坐标分割或全文本提取。

