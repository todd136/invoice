# 发票坐标分区功能分析

## 一、Go项目中的坐标分区实现

### 1.1 核心函数：`partitionInvoiceZones`

**位置**: `/Volumes/share/project/go/financial/internal/service/invoice_table_parser.go:61-338`

**功能**: 通过坐标位置将发票分为4个区域：
- **发票头（Header）**: 包含发票号码、开票日期等
- **购买方（Buyer）**: 左侧区域
- **销售方（Seller）**: 右侧区域  
- **表格（Table）**: 商品明细表格区域

### 1.2 分区策略

#### 1.2.1 上下分区（Y坐标）

1. **查找表头行Y坐标**:
   - 从表格结构中查找包含"项目名称"等关键词的表头行
   - 或直接从文本中查找包含至少2个表头关键词的文本
   - 记录表头行的Y坐标作为表格区域的起始点

2. **识别头部区域**:
   - 关键词: `["发票号码", "开票日期", "电子发票", "普通发票"]`
   - 收集包含这些关键词的文本的Y坐标
   - 计算头部区域的Y边界: `headerYStart` 和 `headerYEnd`

3. **识别购销方区域**:
   - 关键词: `["购买方信息", "销售方信息", "购买方", "销售方", "名称", "统一社会信用代码", "纳税人识别号"]`
   - 收集包含这些关键词的文本的Y坐标
   - 计算购销方区域的Y边界: `buyerSellerYMax` 和 `buyerSellerYMin`
   - 确保购销区在Header下方、表格上方

4. **表格区域**:
   - 表头行Y坐标及以下的所有文本

#### 1.2.2 左右分区（X坐标）

1. **计算页面X范围**:
   - 遍历所有文本，找到最小X和最大X
   - 计算中点: `MidX = (minX + maxX) / 2`

2. **购买方/销售方区分**:
   - 购买方关键词: `["购买方", "购 买 方", "名称", "统一社会信用代码", "纳税人识别号"]`
   - 销售方关键词: `["销售方", "销 售 方", "名称", "统一社会信用代码", "纳税人识别号"]`
   - 如果文本包含购买方关键词且 `x <= MidX` → 归入购买方
   - 如果文本包含销售方关键词且 `x > MidX` → 归入销售方
   - 否则按X坐标分配：`x <= MidX` → 购买方，`x > MidX` → 销售方

### 1.3 文本重建与校正

1. **按区域重建文本**:
   - 对每个区域的文本块按Y-X排序
   - 合并同一行的文本块
   - 使用参考文本（cleanedText）校正顺序

2. **关键字段校正**:
   - `correctHeaderFieldsWithReference`: 校正发票头区域的关键字段（发票号码、发票类型）
   - `correctBuyerFieldsWithReference`: 校正购买方区域的关键字段（购买方名称）

3. **从分区文本中提取信息**:
   - 发票号码、开票日期：从Header区域提取
   - 购买方名称、税号：从Buyer区域提取
   - 发票类型：从Header区域提取

---

## 二、Python项目当前实现分析

### 2.1 当前实现方式

**位置**: `src/invoice/invoice_service.py`

**当前流程**:
1. 使用 `page.extract_words()` 提取单词（包含坐标信息）
2. 过滤红色印章文字
3. 将所有单词合并为 `full_text`
4. 从 `cleaned_text` 中直接提取所有基本信息（未使用坐标分区）

**问题**:
- ❌ 没有使用坐标进行分区
- ❌ 所有字段都从整个 `cleaned_text` 中提取，可能误匹配
- ❌ 无法利用区域信息提高提取准确性

### 2.2 pdfplumber提供的坐标信息

**`extract_words()` 返回的word字典包含**:
```python
{
    'text': '发票号码',
    'x0': 100.5,      # 左边界
    'x1': 150.2,      # 右边界
    'top': 50.3,      # 上边界（Y坐标，从上到下）
    'bottom': 65.8,   # 下边界
    'width': 49.7,    # 宽度
    'height': 15.5,   # 高度
}
```

**`page.chars` 包含字符级别的坐标**:
```python
{
    'text': '发',
    'x0': 100.5,
    'x1': 115.2,
    'top': 50.3,
    'bottom': 65.8,
    'non_stroking_color': (0, 0, 0),  # RGB颜色
    'stroking_color': None,
}
```

**结论**: pdfplumber完全支持坐标信息，可以实现类似Go项目的分区功能。

---

## 三、在Python中实现坐标分区的可行性分析

### 3.1 ✅ 完全可行

**理由**:
1. **坐标信息可用**: `extract_words()` 返回的每个word都包含 `x0`, `x1`, `top`, `bottom` 坐标
2. **功能对等**: pdfplumber提供的坐标信息与Go项目中的pdf库功能对等
3. **实现路径清晰**: 可以完全参考Go项目的实现逻辑

### 3.2 实现方案

#### 3.2.1 核心函数设计

```python
def partition_invoice_zones(words: List[dict], table: Optional[List[List]], page) -> Tuple[List[dict], List[dict], List[dict], List[dict]]:
    """
    通过坐标位置将发票分为4个区域
    
    Args:
        words: extract_words()返回的单词列表（包含坐标信息）
        table: 提取的表格数据
        page: pdfplumber Page对象
    
    Returns:
        (header_words, buyer_words, seller_words, table_words)
    """
    # 1. 计算页面X范围和中点
    # 2. 查找表头行Y坐标
    # 3. 识别各区域的Y边界
    # 4. 按坐标分配单词到各区域
    pass
```

#### 3.2.2 分区逻辑

1. **查找表头行Y坐标**:
   ```python
   # 从表格中查找
   table_keywords = ['项目名称', '规格型号', '单位', '数量', '单价', '金额', '税率', '税额']
   for row in table:
       if sum(1 for kw in table_keywords if kw in ' '.join(row)) >= 3:
           table_header_y = row[0]['top']  # 假设可以从表格获取Y坐标
           break
   
   # 或从words中查找
   for word in words:
       if sum(1 for kw in table_keywords if kw in word['text']) >= 2:
           table_header_y = word['top']
           break
   ```

2. **识别头部区域Y边界**:
   ```python
   header_keywords = ['发票号码', '开票日期', '电子发票', '普通发票']
   header_y_coords = [w['top'] for w in words if any(kw in w['text'] for kw in header_keywords)]
   if header_y_coords:
       header_y_start = max(header_y_coords) + 10
       header_y_end = min(header_y_coords) - 5
   ```

3. **识别购销方区域Y边界**:
   ```python
   buyer_seller_keywords = ['购买方', '销售方', '名称', '统一社会信用代码', '纳税人识别号']
   buyer_seller_y_coords = [w['top'] for w in words if any(kw in w['text'] for kw in buyer_seller_keywords)]
   if buyer_seller_y_coords:
       buyer_seller_y_max = max(buyer_seller_y_coords) + 10
       buyer_seller_y_min = min(buyer_seller_y_coords) - 20
   ```

4. **按坐标分配单词**:
   ```python
   header_words = []
   buyer_words = []
   seller_words = []
   table_words = []
   
   # 计算X中点
   all_x = [w['x0'] for w in words]
   mid_x = (min(all_x) + max(all_x)) / 2
   
   for word in words:
       # 表格区
       if table_header_y > 0 and word['top'] <= table_header_y + 5:
           table_words.append(word)
       # 头部区
       elif header_y_end <= word['top'] <= header_y_start:
           header_words.append(word)
       # 购销方区
       elif buyer_seller_y_min <= word['top'] <= buyer_seller_y_max:
           if word['x0'] <= mid_x:
               buyer_words.append(word)
           else:
               seller_words.append(word)
   ```

#### 3.2.3 文本重建

```python
def reconstruct_text_from_words(words: List[dict]) -> str:
    """
    从单词列表重建文本（按Y-X排序）
    """
    # 按Y坐标分组（同一行的单词）
    lines = {}
    for word in words:
        y = round(word['top'], 1)  # 容差处理
        if y not in lines:
            lines[y] = []
        lines[y].append(word)
    
    # 按Y坐标排序（从上到下）
    sorted_lines = sorted(lines.items(), key=lambda x: x[0], reverse=True)
    
    # 每行内按X坐标排序（从左到右）
    result_lines = []
    for y, line_words in sorted_lines:
        sorted_words = sorted(line_words, key=lambda w: w['x0'])
        line_text = ' '.join([w['text'] for w in sorted_words])
        result_lines.append(line_text)
    
    return '\n'.join(result_lines)
```

#### 3.2.4 从分区文本中提取信息

```python
# 重建各区域文本
header_text = reconstruct_text_from_words(header_words)
buyer_text = reconstruct_text_from_words(buyer_words)
seller_text = reconstruct_text_from_words(seller_words)
table_text = reconstruct_text_from_words(table_words)

# 从各区域提取信息
code = extract_invoice_code(header_text, cleaned_text)  # 优先从header_text
date = extract_date(header_text, cleaned_text)          # 优先从header_text
buyer = extract_buyer_name(buyer_text, cleaned_text)   # 优先从buyer_text
buyer_tax_id = extract_buyer_tax_id(buyer_text, cleaned_text)  # 优先从buyer_text
invoice_type = extract_invoice_type(header_text, cleaned_text)  # 优先从header_text
```

### 3.3 优势分析

#### 3.3.1 提高提取准确性

1. **减少误匹配**:
   - 发票号码只从Header区域提取，不会误匹配表格中的数字
   - 购买方名称只从Buyer区域提取，不会误匹配销售方名称

2. **处理乱序文本**:
   - 通过坐标重建文本，可以纠正PDF文本提取时的乱序问题
   - 参考Go项目的 `correctHeaderFieldsWithReference` 和 `correctBuyerFieldsWithReference`

3. **更好的容错性**:
   - 如果某个区域提取失败，可以回退到整个文本提取
   - 分区文本可以作为参考文本校正提取结果

#### 3.3.2 与现有代码兼容

- ✅ 可以保留现有的 `extract_basic_info()` 函数作为备选方案
- ✅ 新增 `extract_basic_info_from_zones()` 函数实现坐标分区提取
- ✅ 在 `extract_invoice_by_table_and_text()` 中优先使用坐标分区，失败时回退到现有方案

### 3.4 实现难点

#### 3.4.1 表格Y坐标获取

**问题**: `extract_table()` 返回的表格数据不直接包含Y坐标信息

**解决方案**:
1. 从 `words` 中查找表头关键词，获取Y坐标
2. 或使用 `page.extract_tables()` 获取表格对象，可能包含坐标信息
3. 或通过表头关键词在words中的位置推断

#### 3.4.2 坐标容差处理

**问题**: PDF坐标可能有微小误差，需要容差处理

**解决方案**:
```python
# Y坐标容差（同一行的单词）
y_tolerance = 3.0

# 判断两个单词是否在同一行
def is_same_line(word1, word2, tolerance=3.0):
    return abs(word1['top'] - word2['top']) < tolerance
```

#### 3.4.3 文本顺序校正

**问题**: PDF文本提取可能乱序，需要参考 `cleaned_text` 校正

**解决方案**:
- 参考Go项目的 `correctStackedBlocksOrder` 和 `reorderBlocksByReference`
- 计算分区文本与参考文本的覆盖率
- 如果覆盖率足够高，使用参考文本校正顺序

---

## 四、实现建议

### 4.1 分阶段实现

**阶段1: 基础分区功能**
- 实现 `partition_invoice_zones()` 函数
- 实现 `reconstruct_text_from_words()` 函数
- 验证分区效果

**阶段2: 分区提取**
- 实现 `extract_basic_info_from_zones()` 函数
- 从各分区文本中提取基本信息
- 与现有提取函数对比，验证准确性提升

**阶段3: 文本校正**
- 实现参考文本校正功能
- 处理乱序文本问题
- 优化提取准确性

### 4.2 代码结构建议

```python
# invoice_service.py

def extract_invoice_by_table_and_text(pdf_file_path: str) -> Invoice:
    # ... 现有代码 ...
    
    # 尝试使用坐标分区提取（新增）
    try:
        header_words, buyer_words, seller_words, table_words = partition_invoice_zones(
            clean_words, table, page
        )
        code, date, buyer, buyer_tax_id, invoice_type = extract_basic_info_from_zones(
            header_words, buyer_words, cleaned_text, full_text
        )
        # 如果提取成功，使用分区结果
        if code and date:
            invoice.code = code
            invoice.date = date
            invoice.buyer = buyer
            invoice.buyer_tax_id = buyer_tax_id
            invoice.invoice_type = invoice_type
    except Exception as e:
        logging.warning(f'坐标分区提取失败，使用备选方案: {e}')
        # 回退到现有方案
        code, date, buyer, buyer_tax_id, invoice_type = extract_basic_info(
            clean_words, cleaned_text, full_text
        )
        invoice.code = code
        invoice.date = date
        invoice.buyer = buyer
        invoice.buyer_tax_id = buyer_tax_id
        invoice.invoice_type = invoice_type
```

### 4.3 测试建议

1. **单元测试**: 测试分区函数是否正确识别各区域
2. **集成测试**: 测试从分区文本中提取信息的准确性
3. **对比测试**: 对比分区提取和全文本提取的准确性

---

## 五、总结

### 5.1 可行性结论

✅ **完全可行** - pdfplumber提供了足够的坐标信息，可以完全实现Go项目中的坐标分区功能。

### 5.2 实现价值

1. **提高准确性**: 通过区域限制减少误匹配
2. **处理乱序**: 通过坐标重建文本，纠正PDF提取乱序
3. **更好的容错**: 分区提取失败时可以回退到现有方案

### 5.3 建议

**建议实现坐标分区功能**，理由：
1. Go项目已经验证了这种方法的有效性
2. Python项目具备实现条件（pdfplumber支持坐标）
3. 可以提高提取准确性，特别是处理复杂格式的发票
4. 可以与现有方案并存，提供更好的容错性

**实现优先级**: 中高（可以显著提升提取准确性，但需要一定开发工作量）


