# 发票模版 1（当前代码所适配的发票类型）

本文档由代码分析得出，描述当前解析逻辑所针对的**一类发票**的版面与字段约定。符合该模版的 PDF 发票可被正确解析；其他版式可能需要扩展或新增模版。

---

## 1. 总体布局

发票页面在**视觉上**分为四个区域，解析顺序为：

| 区域     | 位置           | 用途                     |
|----------|----------------|--------------------------|
| 发票头   | 页面顶部       | 发票号码、开票日期、发票类型 |
| 购买方   | 发票头下方左侧 | 名称、统一社会信用代码/纳税人识别号 |
| 销售方   | 发票头下方右侧 | 名称、税号等（当前仅用购买方） |
| 表格     | 中下部         | 商品明细表头 + 明细行     |

分区依赖以下两者结合：

- **水平线**：发票头底部、表格顶部的横线（宽度约 ≥ 70% 页面宽），用于确定三大块边界。
- **关键行**：含“开票日期”的行作为发票头下界，含“项目名称”的行作为表格上界；二者之间的行视为购销方。

表格底部通过“价税合计”“合计”“备注”“开票人”等关键词或对应水平线截断，其下方的价税合计、备注等不再参与明细解析。

---

## 2. 发票头区域（Header）

### 2.1 必须/常见元素

- **发票号码**
  - 格式：`发票号码` 或 `发票编码` 后跟 **12～20 位数字**。
  - 正则：`发票(?:号码|编码)?[：:\s]*(\d{12,20})`（严格）；宽松模式允许中间有非数字。

- **开票日期**
  - 格式：`开票日期` 后跟 **YYYY年MM月DD日**（年/月/日可带空格）。
  - 正则：`开票日期[：:\s]*(\d{4}年\d{1,2}月\d{1,2}日)`；宽松：`\d{4}\s*年\s*\d{1,2}\s*[月⽉]\s*\d{1,2}\s*[日⽇]`。

- **发票类型**（二选一）
  - 普通发票：文中出现“（普通发票）”或“(普通发票)”或“普通”+“发票”。
  - 增值税专用发票：文中出现“（增值税专用发票）”或“增值税专用发票”或“专用发票”等。

### 2.2 分区与识别方式

- 发票头包含关键词：`发票号码`、`开票日期`、`电子发票`、`普通发票`、`增值税专用发票`。
- 发票头下界：含“开票日期”的行的 Y 坐标（若日期在下一行，则下界会扩展到该日期行）。

---

## 3. 购买方区域（Buyer）

### 3.1 名称

- 首选：`名称：` 或 `名称:` 之后、在“统一社会信用代码”或“纳税人识别号”之前的文本。
- 备选：公司名模式（含“公司”“有限”“股份”“集团”等）或房间号模式（如 `\d+-\d+`、`[A-Za-z]?\d{3,6}`）。
- 会截断到“统一社会信用代码”“纳税人识别号”“销售方”等关键词前；若含“公司”，会截到最后一个“公司”为止。

### 3.2 统一社会信用代码/纳税人识别号

- 格式：**15～20 位**字母数字（允许空格，会规范化后校验）。
- 识别：先匹配“统一社会信用代码”“纳税人识别号”等标签后的内容，再在购买方区域内做全局 15～20 位字母数字扫描，经税号校验与评分后取最佳候选。

### 3.3 购销方分界

- 优先用页面中部附近的**垂直线**（在购销方 Y 范围内）作为购买方/销售方分界。
- 若无垂直线，则用同一行中两个“名称”的 X 坐标中点作为分界；再不行则按页面 X 中点划分。

---

## 4. 表格区域（Table）

### 4.1 表头行

- 表头行必须包含以下 **至少 4 个** 列名（来自 `TABLE_KEYWORDS`）：
  - `项目名称`
  - `规格型号`
  - `单位`
  - `数量`
  - `单价`
  - `金额`
  - `税率`
  - `税额`

- 表头用于：
  - 确定“表格”区域的起始行；
  - 通过表头词的 X 坐标得到各列的 x 范围（含容差），用于把每个单元格内的文字归到对应列。

- 列名可能被拆成多段（如“单”“位”）；代码会通过 X 相邻（距离 < 30 像素）做“拆分关键词”匹配。

### 4.2 明细行（数据行）

- 每一行按列 x 范围，将单词归入对应列，得到：
  - **项目名称**、**规格型号**、**单位**、**数量**、**单价**、**金额**、**税率**、**税额**。

- **项目名称** 的识别规则：
  - 优先：落在“项目名称”列内的文本；
  - 若列内无有效内容，则取本行最左侧、且未被其他列“占位”的中文内容；
  - 期望格式：**\*项目名称\*费用名称**（如 `*金属制品*绿林`），正则：`\*[^*]+\*[^*]+`。

- 以下行会被**跳过**（不作为明细）：
  - 含有“合计”“价税合计”“备注”“开票人”等词的行（`SUMMARY_ROW_KEYWORDS`）。

### 4.3 跨行与折行合并

- **项目名称首行**：以 `*` 开头且符合 `*商品名*费用名` 格式的行，开启一条新商品；后续无金额的行会合并到该项目名称下（项目名称跨行）。
- **规格型号折行**：仅含规格、无金额的行会合并到上一条商品。
- **单字段行**：只有单价/数量等单列、无金额的行会与上一条互补合并。
- **金额行**：有金额的行会补到当前合并中的商品；若当前商品已有金额，则结束合并，该金额行作为新商品起点。
- 多页发票：每页先按上述规则得到“原始明细行”，再在所有页之间用同一套列 x 坐标做跨行合并。

### 4.4 表格底部过滤

- 以下内容会被排除在明细之外（不参与列坐标计算）：
  - 含“价税合计”“合计”“备注”“开票人”“收款人”“复核人”“开户银行”“银行账号”等的行；
  - 多页时：每个“项目名称”表头对应的明细区结束于“小计”“¥”或“合计”行；最后一个小计/合计之后的内容全部过滤。

---

## 5. 其他约定

### 5.1 PDF 与页面

- 使用 **pdfplumber** 提取文本与线条；依赖**可选的文本层**（非纯图片扫描件）。
- 默认处理**所有页**；第一页用于确定基本信息与列坐标，后续页只贡献表格区内的明细行。

### 5.2 印章与颜色

- **红色文字**（RGB 中 R>0.6, G<0.4, B<0.4，或 CMYK 中 M>0.5）会被过滤，不参与识别。
- 例外：含有“增值税”“普通发票”的文字不按红色过滤，避免误删发票类型。

### 5.3 线条

- 用于分区的线：长度 ≥ 30% 页面宽；非红色；表格底部的线会根据“价税合计”等关键词排除，不参与分区。

---

## 6. 输出数据结构（与模版对应）

| 字段           | 来源区域 | 说明 |
|----------------|----------|------|
| 发票号码       | 发票头   | 12～20 位数字 |
| 开票日期       | 发票头   | YYYY年MM月DD日 |
| 购买方         | 购买方   | 名称 |
| 购买方税号     | 购买方   | 统一社会信用代码/纳税人识别号 |
| 发票类型       | 发票头   | 普/专 |
| 明细           | 表格     | 见下表 |

每条明细（LineItem）字段：

| 字段     | 表头列名 | 说明 |
|----------|----------|------|
| item_name | 项目名称 | 建议 *项目*费用 格式 |
| spec     | 规格型号 | |
| unit     | 单位     | |
| quantity | 数量     | |
| price    | 单价     | |
| amount   | 金额     | |
| tax_rate | 税率     | |
| tax_amount | 税额   | |

---

## 7. 小结：本模版适用条件

- 增值税电子/纸质发票（普通或专用），版面为：**发票头 + 购买方/销售方左右分栏 + 下方明细表**。
- 表头至少包含 4 个列名：项目名称、规格型号、单位、数量、单价、金额、税率、税额。
- 发票号码、开票日期、购买方名称与税号、发票类型均出现在上述分区内且格式符合上述规则。
- 项目名称建议为 **\*项目名称\*费用名称** 形式，以便首行与跨行合并正确。

若实际发票在版面、表头列名或关键字段格式上与此模版不一致，解析可能失败或需在代码中增加新模版或分支逻辑。
